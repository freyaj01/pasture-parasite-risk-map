services:
  - type: web
    name: parasite-risk-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn weather_api:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0